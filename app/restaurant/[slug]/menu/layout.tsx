export const metadata = {
  title: "Milestone Grill Menu",
};

const RestaurantMenuLayout = ({ children }: { children: React.ReactNode }) => {
  return <main>{children}</main>;
};

export default RestaurantMenuLayout;
